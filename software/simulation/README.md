# Simulation

Simulation assets and scenarios. PX4 SITL is the default simulator for v2.

## Quick start

From the repo root:

```bash
export PX4_DIR=~/src/PX4-Autopilot
./scripts/sim_px4.sh
```

Then run the mission demo:

```bash
./scripts/run_mission_demo.sh
```
